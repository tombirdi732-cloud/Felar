# Copy to .env and fill in real values:  cp .env.example .env
#
# Port the Node server listens on (behind nginx/Caddy this stays internal).
PORT=3000

# Secret used to sign login tokens. MUST be long and random in production.
# Generate one:  openssl rand -hex 32
JWT_SECRET=change-me-to-a-long-random-string

# Where the SQLite database file is stored (absolute path recommended in prod).
ROOST_DB=/opt/roost/data/roost.db

# --- Calls (WebRTC) ---
# Public STUN is used automatically. For reliable calls across strict NATs,
# add a TURN server (e.g. self-hosted coturn) below. Optional but recommended.
# ROOST_TURN_URL=turn:turn.твойдомен.ru:3478
# ROOST_TURN_USERNAME=roost
# ROOST_TURN_CREDENTIAL=some-secret
